# 小说世界观百科网站

这是一个用于构建小说世界观、角色、势力、历史和关系导图的空框架站点。

第一版只包含栏目、后台配置、数据模型、关系导图框架和测试，不包含任何小说正文设定内容。

## 常用命令

```powershell
npm install
npm run dev
npm run test
npm run build
```

## 内容管理

- 公开内容存放在 `src/content/`。
- 图片存放在 `public/uploads/`。
- 后台入口为 `/admin/`。
- 后台使用 Decap CMS，通过 GitHub 提交内容变更。

